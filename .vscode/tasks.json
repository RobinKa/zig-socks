{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build zig",
            "type": "shell",
            "command": "zig build",
            "group": "build",
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "problemMatcher": []
        }
    ]
}